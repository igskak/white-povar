# Generated native asset candidates

These are build/release inputs only. An installed mobile app cannot receive icon or splash changes through runtime BrandConfig. Review, rasterize to platform-specific sizes, and ship them through the native release pipeline.
