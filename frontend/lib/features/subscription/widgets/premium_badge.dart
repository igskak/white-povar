library;

/// Premium affordances moved into the shared design system so recipe, home and
/// camera surfaces render one implementation. Re-exported here for the existing
/// import sites.
export '../../../core/widgets/premium.dart'
    show PremiumBadge, PremiumOverlay, PremiumIndicator, PremiumGateCard;
